{% extends "base.html.twig" %}
{% block title %}
    Bienvenue sur SymBnb
{%
endblock %}
{% block body %}
    <div class="container mt-4">
        <div class="jumbotron lead">
            <h1 class="display-3">Bienvenue sur SymBnb</h1>
            <p class="lead">
                Nous mettons en relation les voyageurs et les propriétaires qui veulent profiter d'opportunités et gagner de l'argent en propsant leurs biens à la
                                                                                                                                                                                                                                                                      location !
            </p>
            <hr class="my-4"/>
            <p>
                Vous êtes voyageurs ? Et vous voulez voir où vous pourrez loger pour votre
                                                                                                                                                                                                                                                                      prochain déplacement ?
            </p>
            <p class="lead">
                <a class="btn btn-success btn-lg mt-4" href="{{ path('ads_index') }}" role="button">Voir les annonces</a>
            </p>
        </div>

        <h2 class="h1 my-5">Les meilleurs apparts</h2>

        <div class="row mt-4">
            {% for data in ads %}
                {% include "ad/_ad.html.twig" with {'ad': data.annonce} %}
            {% endfor %}
        </div>

        <h2 class="h1 my-4">Nos propriétaires stars</h2>
        <div class="row">
            {% for data in users %}
                <div class="col">
                    <div class="card border-secondary mb-3">
                        <div class="card-body">
                            <h4 class="card-title">{{data.user.fullname}}</h4>

                            <hr/>

                            <div class="card-text">
                                <img alt="avatar" class="float-left mr-3 avatar-medium" src="{{ data.user.picture }}"/>
                                <p>
                                    {{data.user.introduction}}
                                </p>
                                <a class="btn btn-success float-right" href="{{path('user_show', {'slug': data.user.slug}) }}">En savoir plus</a>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}

        </div>
    </div>
{% endblock %}
